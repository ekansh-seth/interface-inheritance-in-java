public class array_7 {// print only maximum element
    public static void main(String[] args) {
        int[] arr = { 8,4,1 };
        int max = arr[0];
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] > max - arr[i]);
        }
        System.out.print(max);
    }
}
